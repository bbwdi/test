
#version 1.0
此框架为web自动化测试框架，具有模拟手工操作页面，数据库连接，测试报告生成，日志记录，自动发送邮件等功能。
框架为每个要操作的页面都定义了一个模块，模块内包含一个继承自基础页面类的页面类，里面包含该页面拥有的元素
及操作方法。在编写自动化测试用例时，对页面的所有操作可通过页面对象进行，复杂用例可先在流程模块先编写好各
条业物流，然后在测试用例中引用定义好的流程。用例执行完毕会自动生成本地测试报告，并邮件发送给相关人员。